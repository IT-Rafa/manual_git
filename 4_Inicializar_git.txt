INICIALIZAR GIT


Con git instalado, lo primero es convertir una carpeta, que será el repositorio de nuestro proyecto de software, a un repositorio git.
Podemos crear la carpeta directamente o usar un generador de proyectos con un IDE o las herramientas del programa.


INICIAR NUEVO REPOSITORIO GIT LOCAL

Entramos en la carpeta y, con la terminal usamos la herramienta git, con la opción init
	git init

Nos dará un mensaje informándonos:
	Initialized empty Git repository in E:/source_dev/manuals/git/git_manual/carpeta/.git/

Con esto ya tenemos un repositorio git, con su carpeta .git incluida, y una rama, creada por defecto, con el nombre main.
Esta carpeta sigue funcionando como una carpeta normal, pero ahora es revisada por el programa git, con lo que podemos usar sus herramientas.


REPOSITORIO GIT

Concretamente, este repositorio git es lo que llamamos un repositorio local.
Los repositorios locales nos permiten modificar el proyecto localmente, es decir que los cambios se harán solo en nuestro equipo, y serán usados por un usuario.

El usuario, normalmente con su email, puede ser configurado solo para este repositorio, o de forma general.





En este repositorio podremos crear, modificar y eliminar los archivos y git tendrá estos archivos controlados.





	
CLONAR REPOSITORIO GIT A LOCAL

Si lo que queremos es una copia del repositorio para usar nosotros como repositorio local.
	
REPOSITORIO GIT

Con esto ya tenemos un repositorio git, con su carpeta .git incluida, y una rama, creada por defecto, con el nombre main.
Concretamente, este repositorio git es lo que llamamos un repositorio local.


Una herramienta de git muy útil es git status, que nos informa del estado actual del repositorio




En git, los repositorios pueden ser locales o remotos.
	- El repositorio local es en el cual podemos crear y modificar los archivos 
	- Los repositorios remotos nos permiten tener los datos coordinados, ya sea en caso de varios usuarios, que nos envien una versión o arreglo,
	o el mismo usuario que usa distintos equipos para hacer los cambios.
	
Para crear un repositorio local, podemos crearlo directamente o hacer una copia de un repositorio remoto.


Crear repositorio local desde 0

Antes de nada necesitamos instalar git. Seguimos desde ahí.

Lo principal es tener una carpeta, que llamaremos se llamará repositorio del proyecto. La podemos crear o usar una con datos ya introducidos.

Dentro de esa carpeta abriremos un terminal e introducimos 
git init